ext {

    // compile

    compile_sdk = 29
    build_tool_version = "29.0.2"

    // configuration

    min_sdk = 21
    target_sdk = 29

    // core

    app_compat_version = "1.1.0"
    kotlin_core_version = "1.1.0"

    // looking glass
    lookinglass_core_version = "dev.1"
}